{
  "sample-seconds": {
    "type": "spinbutton",
    "default": 1,
    "min": 1,
    "max": 10,
    "units": "s",
    "description": "Sampling interval",
    "tooltip": "How often the desklet should sample network counters."
  },
  "selection-mode": {
    "type": "combobox",
    "default": "auto",
    "description": "Interface selection mode",
    "options": {
      "Auto-detect": "auto",
      "Preferred interface": "preferred"
    }
  },
  "preferred-interface": {
    "type": "entry",
    "default": "",
    "description": "Preferred interface",
    "tooltip": "Interface name such as enp3s0 or wlo1. Used when selection mode is Preferred interface."
  },
  "include-tunnel-interfaces": {
    "type": "switch",
    "default": false,
    "description": "Include VPN and tunnel interfaces in auto selection",
    "tooltip": "Allow auto-detection to select interfaces such as tun0 or wg0."
  },
  "visible-interfaces": {
    "type": "entry",
    "default": "",
    "description": "Visible interfaces",
    "tooltip": "Comma-separated interface names. Leave blank to show all automatically discovered interfaces."
  },
  "show-group-all": {
    "type": "switch",
    "default": true,
    "description": "Show Group All Interfaces row",
    "tooltip": "Display the aggregate row for the currently visible interfaces."
  },
  "show-header": {
    "type": "switch",
    "default": true,
    "description": "Show desklet header",
    "tooltip": "Display the desklet title above the content."
  }
}